## 欧拉计划

### 第三题	最大质因数

```python
import math
def Factor(x):
    for index in range(2,int(math.sqrt(x)+1)):
        if(x%index==0):
            return False
    return True
num = int(input("请输入一个数"))
tmp = num
i = 2
while tmp!= 1:
    if tmp%i==0 and Factor(i):
        tmp /= i
        print(i)
    else:
            i = i + 1
#print(i)
```

### 第四题 回文数

```c++
#include<iostream>
#include<vector>
#include<string>
#include<algorithm>
using namespace std;
bool huiwen(int i)
{
	vector<int> str;
	while (i > 0)
	{
		str.push_back(i % 10);
		i /= 10;
	}
	vector<int> str2(str);
	reverse(str.begin(), str.end());
	return str == str2;
}
int main()
{
	bool flag = 1;
	int max = 0;
	for (int i = 999; i >= 100; i--)
	{
		for (int j = 999; j >= 100; j--)
		{
			if (huiwen(i * j))
			{
				if (max < i * j)
				{
					max = i * j;
				}
			}
		}
	}
	cout << max << endl;
}
```

```python
def huiwen(i):
    return str(i)==str(i)[::-1]
flag = 1
max = 0
for index in range(999,100,-1):
        for index2 in range(999,100,-1):
                if(huiwen(index*index2)):
                    if(index*index2>max):
                        max=index*index2
print(max)     
```

### 第五题 最小倍数

```python
num = 2520
while True:
    index = 0
    for i in range(11,21):
        if num % i ==0:
            index += 1
    if index == 10:
        break
    else:
        num += 2520
print(num)
```

### 第六题 平方和与和平方之差

> 关键数学运算

### 第七题 第10001个质数

欧拉筛法：

```python
def euler_sieve(n):
	is_prime = [True] * (n+1)
	prime = []
	for i in range(2,n+1)
		if is_prime[i]:
			prime.append(i)
		for j in range(len(primes)):
			if i*prime[j]>n
				break
			is_prime[i*primes[j]] = False
			if i % primes[j] == 0:
				break
	return primes
```



```python
def sieve_of_eratosthenes(n):
    prime = [True for i in range(n+1)]
    p = 2
    while p*p <= n:
        if prime[p] == True:
            for i in range(p*p, n+1, p):
                prime[i] = False
        p += 1
    return [p for p in range(2,n+1) if prime[p]]
primes = sieve_of_eratosthenes(110000)
print(primes[10000])
```

### 第八题 十三个数字的乘积

> 将所有数组变成字符串

### 第九题 勾股三元组

```python
for m in range(1,101):
    for n in range(1,10):
        a = m**2 - n**2
        b = 2*m*n
        c = m**2 + n**2
        if(a+b+c==1000 and a**2 + b**2 == c**2):
            print(a)
            print(b)
            print(c)
            print(a*b*c)
            break
```

### 第十题 小于2000000的质数和

一、类比第七题

```python
def sieve_of_eratosthenes(n):
    prime = [True for i in range(n+1)]
    p = 2
    while p*p <= n:
        if prime[p] == True:
            for i in range(p*p,n,p):
                prime[i] = False
        p += 1
    return [p for p in range(2,n+1) if prime[p]]
prime = sieve_of_eratosthenes(11000000)
sum = 0
n = 0
while prime[n] < 2000000:
    n += 1
    sum += prime[n]
print(sum)
```

