# Git

```
git init 	初始化仓库
git add .	添加文件到仓库中
git commit -m "备注"	提交仓库

//将本地仓库与远程仓库相联系
git remote add origin git@github:ybo1/仓库名.git

// 切换master分支
git branch -M master

git push -u origin master
```

